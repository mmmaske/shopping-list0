import { GetPriorityColorPipe } from './get-priority-color.pipe';

describe('GetPriorityColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPriorityColorPipe();
    expect(pipe).toBeTruthy();
  });
});
